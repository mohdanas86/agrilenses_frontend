"use client";

import React from "react";
import ChatBot from "./ChatBot";

const page = () => {
  return (
    <div>
      <ChatBot heightValue="h-screen" />
    </div>
  );
};

export default page;
